name: 'dbt_BNPL'
version: '1.0.0'
config-version: 2

profile: 'dbt_BNPL' # Match this in your ~/.dbt/profiles.yml

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_BNPL:
    # Bronze Layer (Staging) -> Schema: dbt_yourname_stg
    staging:
      +materialized: view
      +schema: stg
    
    # Gold Layer (Marts) -> Schema: dbt_yourname_marts
    marts:
      +materialized: table
      +schema: marts